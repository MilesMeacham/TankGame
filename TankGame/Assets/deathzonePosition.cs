using UnityEngine;
using System.Collections;

public class deathzonePosition : MonoBehaviour {

	// Use this for initialization
	void Start () {
		transform.position = new Vector3(-Screen.width / 2, 0, 0);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
